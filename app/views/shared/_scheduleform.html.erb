<%= simple_form_for (@schedule) do |f| %>
  <%= f.rich_text_area :content, placeholder: "Add your content" %>
  <%= f.input :date, html5: true %>
  <% if current_user.teacher %>
    <%= f.input :public%>
  <% end %>
  <%= f.submit class:'btn btn-primary mt-2 btn-block' %>
<% end %>
